.venv/
.env
__pycache__/
*.pyc
reports/*
# …but keep the weekly-winners track record versioned (backed up, survives a re-clone).
!reports/screen_history.json
logs/
.DS_Store

# local scheduled-run wrapper (has a hardcoded path; not portable)
oneshot_fullscreen.sh
